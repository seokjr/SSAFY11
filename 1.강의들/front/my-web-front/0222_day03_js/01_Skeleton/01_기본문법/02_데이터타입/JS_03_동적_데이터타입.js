/* 동적 데이터 타입 */
// 다양한 값의 대입이 가능

var val = 10;
console.log(val, typeof (val)); // 
val = "hello";
console.log(val, typeof (val)); // 
val = true;
console.log(val, typeof (val)); // 


// 다양한 타입의 값이 대입 가능하고,
// 값이 바뀔 때마다, 그 값에 따라 타입도 변경됨
